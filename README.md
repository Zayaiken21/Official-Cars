# Official Cars — GitHub Pages V12

Public static frontend for Official Cars.

Set `config.js` to the deployed Render API. The frontend fetches live dealers and inventory from Render; dealers are **not hardcoded into the static site**.

Vehicle source buttons use the verified exact dealer listing URL. Analytics is sent to Render without blocking navigation.
