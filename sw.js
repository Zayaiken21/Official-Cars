self.addEventListener('install',e=>{self.skipWaiting()});self.addEventListener('activate',e=>{e.waitUntil(self.clients.claim())});self.addEventListener('push',e=>{let d={title:'Official Cars',body:'New inventory is available.',url:'./#/cars'};try{d={...d,...e.data.json()}}catch{}e.waitUntil(self.registration.showNotification(d.title,{body:d.body,icon:'icon.svg',badge:'icon.svg',data:{url:d.url}}))});self.addEventListener('notificationclick',e=>{e.notification.close();let url=e.notification.data?.url||'./#/cars';e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(cs=>{for(const c of cs)if('focus'in c){c.navigate(url);return c.focus()}return clients.openWindow(url)}))});
